{
  "name": "discovery-to-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.mjs dist",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/dev-server.mjs dist",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {}
}
